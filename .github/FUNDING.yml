# Sponsorship is not set up yet. Uncomment a line once an account exists, and
# GitHub adds a Sponsor button to the repo. Adding a `funding` field to each
# package.json would surface it on npm install too.
#
# github: nickbevers
# custom: ["https://nickbevers.dev"]
